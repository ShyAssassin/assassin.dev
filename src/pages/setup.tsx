import Layout from "@components/layouts/layout";
import Head from "next/head";

export default function Setup() {
    return (
        <Layout>
            <Head>
                <title>Assassin | Setup</title>
                <meta name="description" content="The hardware / software i use to write software and play games" />
            </Head>
            <div>Hello World, setup</div>
        </Layout>
    );
}
